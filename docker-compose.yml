services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: propertyops-sql
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: ${SQL_SA_PASSWORD}
    ports:
      - "1433:1433"
    volumes:
      - propertyops_sql_data:/var/opt/mssql

volumes:
  propertyops_sql_data: